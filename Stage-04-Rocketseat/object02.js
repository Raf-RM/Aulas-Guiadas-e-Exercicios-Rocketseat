// Object

//criando um objeto que possui propriedade e valor
const person = {
  name: "Rafael",
  age: 31,
  weight: 80.5,
  isAthlet: true
}

console.log(person)//imprimindo na tela

console.log(person.name)//pegando somente o valor de uma propriedade
console.log(person.weight)//pegando somente o valor de uma propriedade

console.log(`Nome: ${person.name}, idade: ${person.age}, peso: ${person.weight}`)